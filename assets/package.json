{
  "scripts": {
    "watch": "NODE_ENV=production node build.js --deploy",
    "deploy": "NODE_ENV=production node build.js --deploy",
    "dev": "node build.js"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.13",
    "esbuild": "^0.15.14",
    "esbuild-style-plugin": "^1.6.0",
    "postcss": "^8.4.19",
    "tailwindcss": "^3.2.4"
  }
}